apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: delete-hive-cluster
  namespace: {{ .Values.pipelineNamespace }}
  labels:
    {{- include "pipelines.labels" . | nindent 4 }}
spec:
  description: Destroy a Hive/OpenShift cluster provisioned via ClusterDeployment
  params:
    - name: cluster-name
      type: string
    - name: application
      type: string
    - name: platform
      type: string
    - name: namespace
      type: string
    - name: cluster-role
      type: string
      description: "hub or spoke"
      default: "hub"
  steps:
    - name: delete-hive-cluster
      image: {{ .Values.toolsImage }}
      script: |
        #!/usr/bin/env bash
        set -euo pipefail
        # TODO: wire destroy-hive tasks (clusterdeployment delete, namespace cleanup, etc.)
        echo "TODO: delete Hive cluster $(params.cluster-name)"
        echo "  application=$(params.application) platform=$(params.platform) role=$(params.cluster-role)"
        echo "  namespace=$(params.namespace)"
