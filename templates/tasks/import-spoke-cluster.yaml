apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: import-spoke-cluster
  namespace: {{ .Values.pipelineNamespace }}
  labels:
    {{- include "pipelines.labels" . | nindent 4 }}
spec:
  description: Import the spoke cluster into ACM on the hub after pattern install
  params:
  workspaces:
    - name: kubeconfig
  steps:
    - name: import-spoke
      image: {{ .Values.toolsImage }}
      script: |
        #!/usr/bin/env bash
        set -euo pipefail
        # TODO: import spoke as ManagedCluster on hub (klusterlet addon / import secret)
        echo "TODO: import spoke cluster for"
