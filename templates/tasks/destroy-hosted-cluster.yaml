apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: destroy-hosted-cluster
  namespace: {{ .Values.pipelineNamespace }}
  labels:
    {{- include "pipelines.labels" . | nindent 4 }}
spec:
  description: Destroy HyperShift hosted cluster after a successful hcp test run
  params:
    - name: application
      type: string
  steps:
    - name: destroy-hosted-cluster
      image: {{ .Values.hcpImage | default .Values.toolsImage }}
      script: |
        #!/usr/bin/env bash
        set -euo pipefail
        # TODO: hcp destroy cluster / pipeline-destroy-hypershift steps
        echo "TODO: destroy HyperShift cluster for $(params.application)"
